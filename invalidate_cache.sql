-- Questo script serve solo come promemoria
-- Il cache viene invalidato automaticamente quando si modificano i dati
-- Ma se necessario, puoi forzare un refresh dell'applicazione

-- Per forzare il refresh, vai su:
-- 1. Admin → Opzioni Campi → Ruoli Giocatori
-- 2. Aggiungi temporaneamente un ruolo di test
-- 3. Elimina il ruolo di test
-- 4. Questo forzerà il ricaricamento del cache

SELECT 'Cache invalidation reminder' as note; 